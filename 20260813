과제 과정 중 game_money와 item_shop의 출력값 순서와 item_shop 결과 값이 두배로 나오는 실수가 있었음
게임머니가 들어가야 할 자리에 아이템 숍이 들어가고 비워둬야 할 곳에 숫자를 집어 넣어 오류 벌생
함수 내부 공간 구조 신경쓸 것

local,global
1. **L (Local):** 현재 작동 중인 함수 내부 공간 (가장 높은 우선순위)
2. **E (Enclosing):** 중첩 함수 구조에서 내부 함수를 감싸고 있는 외부 함수의 공간
3. **G (Global):** 함수 바깥쪽이자 프로그램 소스코드 파일 전체의 전역 공간
4. **B (Built-in):** 파이썬이 태생적으로 내장하고 있는 print(), len() 같은 기본 함수 공간 (가장 낮은 우선순위)

베이스 넘버 변수 정의
1차 결과값
1차 결과값에 조건 부여/ filter와 lambda 식을 연동한 후 list 함수로 변환/출력
